{
  "name": "cypress-1",
  "version": "1.0.0",
  "description": "Home Work",
  "main": "index.js",
  "scripts": {
    "testChrome": "npx cypress run --browser chrome --headed --config-file cypress.env.json",
    "testChromeheadless": "npx cypress run --browser chrome --headless",
    "testElectron": "npx cypress run --browser electron --headed --config-file cypress.env.json",
    "testElectronheadless": "npx cypress run --browser electron --headless"
  },
  "author": "Ekaterina",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.4",
    "cypress": "^9.1.0",
    "eslint": "^8.3.0",
    "prettier": "^2.5.0"
  }
}
